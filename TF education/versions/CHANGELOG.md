# 버전 변경 이력 (CHANGELOG)

---

## ver.2.1 (2026-07-06) — 현재 최신
파일: `app_v2.1.py` / `index_v2.1.html`

### 주요 변경
| 항목 | 내용 |
|------|------|
| **매칭 속도 개선** | 자산 3000대 기준 10분 이상 → 수십 초로 단축. 자산별 개별 API 호출 제거, `_build_recall_serial_set()`으로 이진탐색+병렬 일괄 수집으로 전환 |
| **점수 체계 100점 만점화** | 기존 최대치 불명확 → 제조번호(40)+업체명(20)+품목명(30)+분류명(10) 합계 100점 체계 |
| **업체명 유사도 매칭 추가** | 제조사/공급사 vs 회수업체명(ENTP_NAME) 유사도를 매칭 요소로 추가. 니혼코덴↔필립스 오탐 방지 |
| **검토 필요 임계값 상향** | 30점 → **50점** (높은 가능성: 70점 이상 유지) |
| **탭 버튼 UX 개선** | 비선택 상태: 흰 배경+검은 글씨+이모지 원, 선택 상태: 색상 배경+흰 글씨 |
| **테이블 컬럼 정리** | 회수이유·보고일자 컬럼 제거 / 회수업체명 컬럼 추가 |
| **상세조회 모델목록** | 회수 상세 모달에 모델명·제조번호·회수대상량 테이블 표시 (`getSerialNumList01` 이진탐색) |
| **테이블 헤더 고정폭** | 헤더 줄바꿈 방지, 가로 스크롤 지원 |

---

## ver.2.0 (2026-07-01)
파일: `app_v2.0.py` / `index_v2.0.html`

### 주요 변경 (ver.1 대비)
| 항목 | 내용 |
|------|------|
| **식약처 API 회수조회 기능 추가** | `MdlpRtrvlSleStpgeInfoService02` 연동. 품목명/업체명/기간 필터 검색 |
| **회수 목록 캐시** | 조회 결과를 메모리에 캐시해 재조회 없이 매칭 재사용 |
| **자산 매칭 기능 추가** | 엑셀 업로드 → 회수 캐시와 품목명 유사도 매칭 → 높은 가능성/검토 필요 분류 |
| **관리자 모드** | 비밀번호(admin1234) 입력 시 `.admin-only` 메뉴 활성화 |
| **로깅 강화** | 백엔드 API 오류 원문 반환, MFDS 필드명 수정 (SERIAL_NUM→MAKE_NO) |

---

## ver.1.0 (2026-06-11)
파일: git 커밋 `46cb341` 참고

### 주요 기능
| 항목 | 내용 |
|------|------|
| **AI 채팅 기본 구조** | FastAPI(port 8000) + Ollama qwen3.6:35b-a3b 연결, SSE 스트리밍 |
| **단일 HTML UI** | 참고자료 입력, 질문 입력, 실시간 답변 표시, 백엔드 상태 배지 |
| **CORS 지원** | 프론트엔드↔백엔드 분리 구조 |

---

## 버전 되돌리기 방법

```powershell
# 예: ver.2.0으로 되돌리기
Copy-Item "D:\claude ai\TF education\versions\app_v2.0.py"    "D:\claude ai\TF education\app.py" -Force
Copy-Item "D:\claude ai\TF education\versions\index_v2.0.html" "D:\claude ai\TF education\index.html" -Force
# 이후 서버 재시작
python -m uvicorn app:app --port 8000 --reload
```

---

_저장 기준: 각 버전 파일은 저장 시점의 app.py + index.html 스냅샷_
